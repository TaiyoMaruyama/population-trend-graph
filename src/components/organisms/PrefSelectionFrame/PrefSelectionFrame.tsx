'use client';

import PrefCheckboxGroup from '@/components/molecules/PrefCheckboxGroup/PrefCheckboxGroup';

const PrefSelectionFrame = () => {
  return <PrefCheckboxGroup checkedList={[]} handleCheck={() => {}} />;
};

export default PrefSelectionFrame;
